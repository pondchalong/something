ccxt==4.5.56
pandas==3.0.3
# pin ไว้เพราะ streamlit ลากมาเองแบบไม่ระบุเวอร์ชัน — พอ build ใหม่แล้วได้ตัวที่ไม่เข้ากับ
# pandas 3 จะ Segmentation fault (27 ก.ค. 2026). config.py ยังบังคับไม่ให้ pandas ใช้ pyarrow ซ้ำอีกชั้น
pyarrow==24.0.0
pandas-ta==0.4.71b0
python-telegram-bot==22.7
python-dotenv==1.2.2
requests==2.34.2
streamlit==1.58.0
plotly==6.7.0
scikit-learn==1.8.0
numpy==2.2.6
optuna==4.9.0
